import {Container, ControlButton, Label} from "./_base";

const FilterButton = () => (
    <Container id="filter-controller">
        <ControlButton>
            <Label>닫은 약국 숨김</Label>
        </ControlButton>
    </Container>
)
export default FilterButton;